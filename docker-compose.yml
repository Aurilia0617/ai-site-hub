services:
  app:
    build: .
    ports:
      - "3000:8080"
    volumes:
      - site-data:/app/data
    environment:
      - PORT=8080
      - CORS_ORIGIN=*
    restart: unless-stopped

volumes:
  site-data:
