{
  "template": "DOCKERFILE",
  "ports": [8080],
  "volumes": [
    {
      "id": "site-data",
      "dir": "/app/data"
    }
  ],
  "env": {
    "PORT": "8080",
    "CORS_ORIGIN": "*",
    "ACCESS_PASSWORD": "${ACCESS_PASSWORD}"
  }
}
